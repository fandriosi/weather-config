# Docker profile

spring.datasource.url=jdbc:postgresql://${RDSHOST}:${RDSPORT:5432}/${RDS_DB}?sslmode=${RDS_SSLMODE:verify-full}&sslrootcert=${RDS_SSLROOTCERT:/certs/global-bundle.pem}&connectTimeout=${RDS_CONNECT_TIMEOUT:5}&socketTimeout=${RDS_SOCKET_TIMEOUT:30}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${RDSUSER}
spring.datasource.password=${RDSPASSWORD}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

app.security.jwt.secret=${APP_SECURITY_JWT_SECRET}
app.security.jwt.expiration-minutes=${APP_SECURITY_JWT_EXPIRATION_MINUTES:60}
app.iot.ingest.hmac-secret=${APP_IOT_INGEST_HMAC_SECRET}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:*}

app.dynamodb.table-name=${DYNAMODB_TABLE_NAME:WeatherMensuracao}
app.dynamodb.endpoint=${DYNAMODB_ENDPOINT:}
app.dynamodb.access-key=${AWS_ACCESS_KEY_ID:}
app.dynamodb.secret-key=${AWS_SECRET_ACCESS_KEY:}
app.dynamodb.session-token=${AWS_SESSION_TOKEN:}
app.dynamodb.region=${AWS_REGION:us-east-1}
app.storage.type=${APP_STORAGE_TYPE:LOCAL}
app.storage.local.base-path=${APP_STORAGE_LOCAL_BASE_PATH:storage}

logging.level.org.hibernate.SQL=${LOG_LEVEL_HIBERNATE_SQL:INFO}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOG_LEVEL_HIBERNATE_BINDER:INFO}
logging.level.software.amazon.awssdk=${LOG_LEVEL_AWS_SDK:INFO}
logging.level.software.amazon.awssdk.request=${LOG_LEVEL_AWS_SDK_REQUEST:INFO}
